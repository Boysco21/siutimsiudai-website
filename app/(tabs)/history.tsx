import { HistoryScreen } from "@/screens/HistoryScreen";

// Thin route wrapper; the composed logbook body lives in screens/ for reuse and testing.
export default function History() {
  return <HistoryScreen />;
}
